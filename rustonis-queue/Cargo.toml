[package]
name    = "rustonis-queue"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
thiserror   = "1"
tokio       = { version = "1", features = ["sync", "time", "rt", "macros"] }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
